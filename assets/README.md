# Local Assets

Inkumo keeps third-party icon fonts out of Git. Run:

```bash
make setup
```

This downloads the local assets required for a complete render. Generated files
are ignored by `.gitignore`; source notes and fetch scripts are tracked.
